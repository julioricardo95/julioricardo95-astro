<div class="whatsapp-bottom">
  <a href="https://wa.me/message/H4MRQFKG6WBLG1" class="whatsapp-bottom-a">
    <i class="fa-brands fa-whatsapp whatsapp-bottom-i"></i>
  </a>
</div>

<style>
  /* Contenedor principal */
  .whatsapp-bottom {
    position: fixed;
    bottom: 80px;
    right: 25px;
    z-index: 1000;
  }

  /* Enlace del botón */
  .whatsapp-bottom-a {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 50px;
    height: 50px;
    background-color: #25d366;
    border-radius: 50%;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.2);
    transition: all 0.3s ease;
    animation: float 3s ease-in-out infinite;
  }

  /* Ícono de WhatsApp */
  .whatsapp-bottom-i {
    font-size: 32px;
    color: white;
  }

  /* Animación flotante */
  @keyframes float {
    0%, 100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(-10px);
    }
  }

  /* Efecto hover */
  .whatsapp-bottom-a:hover {
    background-color: #128c7e;
    box-shadow: 0 6px 8px rgba(0, 0, 0, 0.3);
    transform: scale(1.1);
  }

  /* Responsive para pantallas pequeñas */
  @media (max-width: 768px) {
    .whatsapp-bottom-a {
      width: 50px;
      height: 50px;
    }

    .whatsapp-bottom-i {
      font-size: 28px;
    }
  }
</style>